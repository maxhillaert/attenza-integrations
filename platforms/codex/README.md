# Codex

The installable package is in [`../../plugins/attenza`](../../plugins/attenza), with a repository marketplace catalog in [`.agents/plugins/marketplace.json`](../../.agents/plugins/marketplace.json).

The plugin adds the public OAuth MCP server and the intervention skill together. Each Codex user authorizes their own Attenza account; neither the plugin nor the marketplace contains a user credential.
