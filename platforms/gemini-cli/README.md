# Gemini CLI

Merge the `mcpServers.attenza` object from `settings.json` into Gemini CLI settings. Keep `trust` disabled until you have reviewed the tools, then complete OAuth when the client prompts.

Host support for OAuth-protected remote MCP may vary by release; do not replace OAuth with a credential committed to settings.
