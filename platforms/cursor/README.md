# Cursor

Copy `mcp.json` into the appropriate Cursor MCP configuration location or install the portable Agent Plugin from `agent-plugins/attenza` when supported by your Cursor version.

Enable the `attenza` server and complete the browser OAuth flow. The same configuration works for local agents, Cloud Agents, and Automations when their environment supports authenticated remote MCP.
