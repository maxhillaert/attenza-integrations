# VS Code

Copy `mcp.json` to `.vscode/mcp.json` in a project or add the same server through VS Code's MCP configuration UI. Start the server and complete Attenza OAuth in the browser.

Repository-level configuration contains only the public endpoint and is safe to commit. OAuth tokens remain in the editor's credential storage.
