# Windsurf

Merge the `attenza` entry from `mcp_config.json` into Windsurf's MCP configuration. Complete OAuth if the installed Windsurf release supports authenticated remote MCP.

If it does not, leave the integration disabled rather than placing a private capability URL in shared configuration.
