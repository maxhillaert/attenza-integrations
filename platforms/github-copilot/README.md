# GitHub Copilot

Use `mcp.json` as a repository or user MCP configuration. Start Copilot, enable the Attenza tools, and complete the OAuth flow when the client opens it.

For unattended coding agents, authorize the connection interactively once in the owning user context and rely on the host's secure token storage and refresh behavior.
