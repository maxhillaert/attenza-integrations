# Attenza for Claude Code

This plugin supplies both the Attenza decision workflow and its public OAuth MCP connection.

```text
/plugin marketplace add maxhillaert/attenza-integrations
/plugin install attenza@attenza
```

Claude Code will open Attenza sign-in and consent when the remote MCP connection is first used. No API key or private connection URL is required.
